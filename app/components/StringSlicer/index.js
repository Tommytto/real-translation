// @flow
import React from "react";
import {View} from "react-native";

type TProps = {
    string: string,
}

export default function StringSlicer({string}: TProps) {
    return <View>

    </View>
}